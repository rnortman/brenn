/// Meeting notice: shows time-to-next-meeting and escalates to a fullscreen
/// takeover. Reads the agenda snapshot, requests takeover, and both publishes
/// and reads dismiss/snooze acks so every device converges — hence `io acks`.
/// Draws its own panel — label, title, countdown, sub-line and a Dismiss/Snooze
/// button row — so it holds `dom`.
///
/// Its state machine, its view handles and the occurrence a button press acks
/// ride the retained `state` port between activations, because linear memory
/// does not.
component Meeting {
  abi = processor;
  requires = [ports, log, takeover, dom];
  in agenda;
  io acks;
  out takeover: "brenn.surface.takeover@1";
  io tick;
  io state;
}
