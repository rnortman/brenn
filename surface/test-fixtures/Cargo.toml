[package]
name = "brenn-surface-test-fixtures"
version = "0.1.0"
edition = "2024"
publish = false
description = "The canonical delivered-envelope JSON fixture shared by the surface test suites"

[dependencies]
brenn-envelope = { path = "../../brenn-envelope" }
serde_json = "1.0.149"
uuid = { version = "1.23.0", features = ["serde"] }
