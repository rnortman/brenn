# Test-only fault-injection component for brenn-wasm integration tests.
# Two sentinel ops: LEAK_TX (leaked transaction → host trap) and TRAP (guest panic → host trap).
# NOT for production use.
#

[package]
name = "brenn-replay-fault-test"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
# Runtime crate for the pinned wit-bindgen generator. The runtime is versioned
# independently of the generator and does not track its version.
wit-bindgen-rt = "0.44"

[package.metadata.component]
package = "brenn:replay"
target = { path = "../../wit/replay.wit" }

[package.metadata.component.dependencies]
