[package]
name = "brenn-git-sync-consumer"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
brenn-guest = { path = "../guest" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
