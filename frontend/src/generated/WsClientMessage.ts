// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AttachmentRef } from "./AttachmentRef";
import type { DebugViewportSnapshotData } from "./DebugViewportSnapshotData";
import type { PermissionDecision } from "./PermissionDecision";
import type { PushClickTraceEvent } from "./PushClickTraceEvent";
import type { SelectedTask } from "./SelectedTask";
import type { TodoAnchor } from "./TodoAnchor";
import type { ToolResponseDecision } from "./ToolResponseDecision";
import type { ViewportClass } from "./ViewportClass";

/**
 * Browser → Backend message.
 *
 * Protocol convention: no `#[serde(other)]` on TS-exported enums.
 * Backend-internal catch-all enums must not derive `TS`.
 */
export type WsClientMessage = { "type": "SendMessage", text: string, attachments: Array<AttachmentRef>, 
/**
 * Model alias to use (e.g. "sonnet", "opus", "haiku", "default").
 * If None, uses the app's configured default model.
 */
model: string | null, 
/**
 * Tasks selected in the todo list to inject as context alongside the message.
 */
selected_tasks: Array<SelectedTask>, } | { "type": "PermissionResponse", request_id: string, decision: PermissionDecision, } | { "type": "ToolCardResponse", request_id: string, decision: ToolResponseDecision, } | { "type": "ClientError", message: string, } | { "type": "PushClickTrace", user_id: number, event: PushClickTraceEvent, } | { "type": "ListConversations" } | { "type": "SwitchConversation", conversation_id: number, } | { "type": "NewConversation" } | { "type": "Reconnect", conversation_id: number, last_seq: number | null, } | { "type": "ReopenArtifact", file_path: string, message_id: number | null, } | { "type": "LoadArtifactSnapshot", message_id: number, } | { "type": "StealApp" } | { "type": "SetTimezone", timezone: string, } | { "type": "SetDeviceInfo", user_agent: string, platform: string, screen_width: number, screen_height: number, } | { "type": "StopRequest" } | { "type": "SetViewportClass", viewport_class: ViewportClass, } | { "type": "SetConversationPrivacy", conversation_id: number, shared: boolean, } | { "type": "RequestCompaction" } | { "type": "RunTarget", 
/**
 * Target name (e.g. "import").
 */
target: string, 
/**
 * Upload IDs from the Phase 1 HTTP upload.
 */
upload_ids: Array<string>, } | { "type": "TodoRefresh" } | { "type": "TodoDone", path: string, 
/**
 * Repo slug (from `TodoItem.repo`). Required for multi-repo manifests.
 */
repo?: string | null, 
/**
 * Browser-local today (authoritative — the backend refuses to
 * guess via server wall-clock because backend and browser can
 * disagree on timezone). Required: the frontend always sends
 * `localTodayStr()` here; serde rejects the message at
 * deserialize if the field is absent.
 */
completion_date: string, } | { "type": "TodoSchedule", path: string, 
/**
 * Repo slug (from `TodoItem.repo`). Required for multi-repo manifests.
 */
repo?: string | null, 
/**
 * ISO 8601 date (YYYY-MM-DD).
 */
date: string, } | { "type": "TodoReorder", path: string, 
/**
 * Repo slug (from `TodoItem.repo`). Required for multi-repo manifests.
 */
repo?: string | null, 
/**
 * Place after this neighbor.
 */
after?: TodoAnchor | null, 
/**
 * Place before this neighbor.
 */
before?: TodoAnchor | null, } | { "type": "LoadMoreHistory", before_seq: number, } | { "type": "PushVapidKeyRequest" } | { "type": "PushSubscribe", endpoint: string, p256dh: string, auth: string, } | { "type": "PushUnsubscribe" } | { "type": "DebugViewportSnapshot", snapshot: DebugViewportSnapshotData, };
